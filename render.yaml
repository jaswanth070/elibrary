services:
  - type: web
    name: Library
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn Library.wsgi:application"
    envVars:
      # - key: DATABASE_URL
      #   fromDatabase:
      #     name: mysite
      #     property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4